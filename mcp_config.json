{
  "mcpServers": {
    "notebook-analyzer": {
      "command": "curl",
      "args": [
        "-X", "POST",
        "http://localhost:8080/mcp",
        "-H", "Content-Type: application/json"
      ],
      "description": "Notebook Analyzer for Jupyter Notebooks",
      "transport": "http",
      "baseUrl": "http://localhost:8080/mcp"
    }
  }
} 